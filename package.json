{
  "name": "ruleadd",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "WebGL 3D Hole-in-the-wall puzzle — dodge approaching walls, rules accumulate each round.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.171.0"
  },
  "devDependencies": {
    "@types/three": "^0.171.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  }
}
